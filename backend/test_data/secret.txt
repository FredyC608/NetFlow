This is a secret financial document

docker exec -it netflow-db psql -U netflow -d netflow_db -c "INSERT INTO users (id, email, hashed_password) VALUES (1, 'admin@netflow.ai', 'placeholder_hash') ON CONFLICT DO NOTHING;"


docker exec -it netflow-db psql -U netflow -d netflow_db -c "SELECT current_database();"


alembic revision --autogenerate -m "Initial_tables" # writes the instructions from the code-db difference


curl.exe -X POST "http://localhost:8000/upload" -F "file=@C:/Users/amirs/Desktop/NetFlow/Netflow/backend/test_data/secret.txt" -F "key=secret"

docker compose exec db bash